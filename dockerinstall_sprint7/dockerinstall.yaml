package:
    name: DockerInstall-KO
    description: Install Docker if not already installed
    version: 1.0.0
    headlessExecution: true
    manifest:
        lifecycle:
            -   phase: execute
                action: dockerinstall.sh

    attachments:
        -   path: dockerinstall.sh
        

